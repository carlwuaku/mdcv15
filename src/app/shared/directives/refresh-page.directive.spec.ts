import { RefreshPageDirective } from './refresh-page.directive';

describe('RefreshPageDirective', () => {
  it('should create an instance', () => {
    const directive = new RefreshPageDirective();
    expect(directive).toBeTruthy();
  });
});
