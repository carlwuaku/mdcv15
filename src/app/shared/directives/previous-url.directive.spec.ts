import { PreviousUrlDirective } from './previous-url.directive';

describe('PreviousUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviousUrlDirective();
    expect(directive).toBeTruthy();
  });
});
